# Demo Docs Repository

This repository is used to test a WordPress plugin that syncs Markdown files from Git into WordPress posts or custom post types.

Structure:

- /handbook contains all publishable content
- Each file uses YAML frontmatter
- One-way sync into WordPress

Do not edit content directly in WordPress.
